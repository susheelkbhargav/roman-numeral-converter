openapi: 3.0.1
info:
  title: OpenAPI definition
  version: v0
servers:
- url: http://localhost:8080
  description: Generated server url
paths:
  /romannumeral:
    get:
      tags:
      - roman-numeral-converter-controller
      operationId: toRomanNum_1
      parameters:
      - name: query
        in: query
        required: true
        schema:
          type: string
      - name: min
        in: query
        required: true
        schema:
          type: string
      - name: max
        in: query
        required: true
        schema:
          type: string
      responses:
        "200":
          description: OK
          content:
            '*/*':
              schema:
                oneOf:
                - $ref: '#/components/schemas/RomanNumeralList'
                - $ref: '#/components/schemas/RomanNumber'
components:
  schemas:
    RomanNumber:
      type: object
      properties:
        input:
          type: integer
          format: int32
        output:
          type: string
    RomanNumeralList:
      type: object
      properties:
        conversions:
          type: array
          items:
            $ref: '#/components/schemas/RomanNumber'
